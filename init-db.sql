-- MySQL 초기화 스크립트

CREATE DATABASE IF NOT EXISTS `satto` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 데이터베이스 사용
USE `satto`;

-- GRANT ALL PRIVILEGES ON `ehr-qa`.* TO 'root'@'%';
-- FLUSH PRIVILEGES;